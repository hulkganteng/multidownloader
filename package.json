{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "npm run build && concurrently --kill-others --names server,queue,schedule \"php artisan serve\" \"php artisan queue:work --tries=3\" \"php artisan schedule:work\"",
        "start:dev": "concurrently --kill-others --names server,queue,schedule,vite \"php artisan serve\" \"php artisan queue:work --tries=3\" \"php artisan schedule:work\" \"vite\""
    },
    "devDependencies": {
        "autoprefixer": "^10.5.4",
        "concurrently": "^10.0.5",
        "laravel-vite-plugin": "^3.2.0",
        "postcss": "^8.5.26",
        "tailwindcss": "^3.4.17",
        "vite": "^8.2.2"
    }
}
